import utils
from utils import lbs_to_kg

print(utils.kg_to_lbs(70))
print(lbs_to_kg(155))

# -------------------------------------- Exercise

numbers = [3, 6, 2, 15, 8, 4, 10]
max_num = utils.find_max_in_list(numbers)
print(f'This is max number: {max_num}')

# --- page 70